#include <iostream>

int main() 
{
	system("color 8E"); // Changes the color of background to grey and th foregroun (letters color) to light yellow
	system("PAUSE"); // Makes the system wait for a keyboard input (press any key except special keys)
	return 0;
}